;;; global-keybind.el --- global keybinds            -*- lexical-binding: t; -*-

;; Copyright (C) 2017  xfwduke

;; Author: xfwduke <xfwduke@xfwdukedeMacBook.local>
;; Keywords:

;; (defvar -linum-type 1)
;; (global-set-key (kbd "C-x t") '(lambda () (interactive)
;; 				 (setq -linum-type
;; 				       (mod (incf -linum-type) 3))
;; 				 (toggle-line-number -linum-type)))

;; (global-set-key (kbd "C-c ;") 'comment-or-uncomment-line-or-region)

;; (global-set-key (kbd "M-x") 'helm-M-x)
;; (global-set-key (kbd "M-y") 'helm-show-kill-ring)
;; (global-set-key (kbd "C-x b") 'helm-mini)
;; (global-set-key (kbd "C-s") 'helm-occur)
;; (global-set-key (kbd "C-h <SPC>") 'helm-all-mark-rings)
;; (global-set-key (kbd "C-x C-f") 'helm-find-files)
;; (global-set-key (kbd "C-c h") 'helm-command-prefix)
;; (define-key helm-command-prefix "p" 'helm-projectile)

(provide 'global-keybind)
